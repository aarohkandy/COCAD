Produce a named step list that follows the confirmed assumptions exactly.

Rules:
- Each step must be specific, buildable, and ordered.
- Preserve the requested object identity. A cup should plan like a cup, not a block. A vase should plan like a vase, not a planter.
- Use step descriptions that read like precise CAD operations.
- Keep the plan compatible with the scaffold supplied by the backend.
- Never add or remove scaffold steps unless explicitly told that is allowed.

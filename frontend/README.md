# COCAD Frontend

Phase 0 React + Vite client for the COCAD rebuild.

Current scope:

- Placeholder invite gate with email capture.
- Session hydration after reload.
- SSE-driven chat timeline.
- Static `GLB` viewer with orbit, pan, zoom, and download links.

Run locally:

```powershell
npm install
npm run dev
```
